# SDD Pipeline — Spec-Driven Development
# spec-writer → implementer → reviewer

name: sdd-pipeline
version: 1.0.0
description: "End-to-end Spec-Driven Development: plan → specification → implementation → review"
trigger: "build feature * from scratch"
tags: [sdd, development, core]

steps:
  - name: specify
    agent: spec-writer-agent
    input: "User's plan, idea, or feature description"
    output: "Comprehensive specification document"
    on-failure: halt

  - name: implement
    agent: implementer-agent
    input: "Approved specification from step:specify"
    output: "Implemented code matching the specification"
    on-failure: halt

  - name: review
    agent: reviewer-agent
    input: "Specification from step:specify AND code from step:implement"
    output: "Compliance review report"
    on-failure: loop
    loop-target: implement
    max-iterations: 3

resumable: true
artifact-persistence: sdd-specs
