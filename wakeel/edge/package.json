{
  "name": "wakeel-edge",
  "private": true,
  "scripts": {
    "build": "python ../scripts/build_index.py && python ../scripts/extract_synapses.py",
    "deploy": "npm run build && wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.103.0"
  }
}
